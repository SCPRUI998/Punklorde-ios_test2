name: Build iOS IPA

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: 1. 拉取仓库代码（浅克隆）
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 1.1 强制清理 macOS 文件扩展属性与修复文件权限
        run: |
          echo "===== 清理 com.apple.provenance 及其他扩展属性 ====="
          # 1. 递归清除所有 macOS 扩展属性
          sudo xattr -cr . 2>/dev/null || true
          
          # 2. 核心修正：将所有文件所有权还给 runner 用户，防止 flutter build 时无权限修改
          sudo chown -R $(whoami) .

      - name: 2. 配置环境变量文件
        run: cp .env.template .env || true

      - name: 3. 安装 Go 语言环境
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: 4. 安装 Rust 编译环境与构建缓存
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios, aarch64-apple-ios-sim, x86_64-apple-ios

      - name: 4.1 将 Cargo/Rust 工具暴露给 Xcode PhaseScript
        run: |
          sudo mkdir -p /usr/local/bin
          sudo ln -sf $HOME/.cargo/bin/cargo /usr/local/bin/cargo
          sudo ln -sf $HOME/.cargo/bin/rustc /usr/local/bin/rustc
          sudo ln -sf $HOME/.cargo/bin/rustup /usr/local/bin/rustup
          sudo ln -sf $HOME/.cargo/bin/* /usr/local/bin/ 2>/dev/null || true

      - name: 4.2 写入 Flutter Xcode 环境配置 (.xcode.env 与 .xcode.env.local)
        run: |
          mkdir -p ios
          cat << 'EOF' > ios/.xcode.env
          export PATH="$HOME/.cargo/bin:/usr/local/bin:/opt/homebrew/bin:$PATH"
          export ENABLE_USER_SCRIPT_SANDBOXING=NO
          EOF
          cp ios/.xcode.env ios/.xcode.env.local

      - name: 4.3 Rust 依赖与编译产物缓存
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            rust -> target

      - name: 5. 安装 Flutter 3.44.4 并禁用 SPM
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'
          channel: 'stable'
          cache: true

      - name: 6. 预处理 Flutter 环境与依赖
        run: |
          echo "===== 配置 ~/.curlrc 规避 CDN 握手重置 ====="
          cat << 'EOF' > ~/.curlrc
          http1.1
          retry = 10
          retry-delay = 2
          retry-max-time = 120
          EOF

          flutter config --no-enable-swift-package-manager

          rm -rf ios/Flutter/ephemeral
          rm -rf ios/Runner.xcworkspace/xcshareddata/swiftpm
          rm -rf ~/Library/Developer/Xcode/DerivedData
          rm -rf build/ios

          sudo xattr -cr . || true

          flutter precache --ios
          flutter pub get

          # 确保 flutter pub get 没有覆写 .xcode.env
          echo "export PATH=\"\$HOME/.cargo/bin:/usr/local/bin:/opt/homebrew/bin:\$PATH\"" >> ios/.xcode.env
          echo "export ENABLE_USER_SCRIPT_SANDBOXING=NO" >> ios/.xcode.env
          cp ios/.xcode.env ios/.xcode.env.local

          # 若存在 build_runner 则执行
          if grep -q "build_runner" pubspec.yaml; then
            dart run build_runner build --delete-conflicting-outputs
          fi

      - name: 7. 动态生成 Podfile 与 Xcode 配置文件
        run: |
          set -e

          echo "===== 生成自定义 Podfile（部署目标提升至 iOS 17.0） ====="
          cat << 'EOF' > ios/Podfile
          use_modular_headers!
          platform :ios, '17.0'

          ENV['COCOAPODS_DISABLE_STATS'] = 'true'

          project 'Runner', {
            'Debug' => :debug,
            'Profile' => :release,
            'Release' => :release,
          }

          def flutter_root
            generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
            unless File.exist?(generated_xcode_build_settings_path)
              raise "#{generated_xcode_build_settings_path} must exist."
            end

            File.foreach(generated_xcode_build_settings_path) do |line|
              matches = line.match(/FLUTTER_ROOT\=(.*)/)
              return matches[1].strip if matches
            end
            raise "FLUTTER_ROOT not found"
          end

          require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

          flutter_ios_podfile_setup

          target 'Runner' do
            pod 'SDWebImage', :modular_headers => true
            flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
          end

          post_install do |installer|
            installer.pods_project.targets.each do |target|
              flutter_additional_ios_build_settings(target)
              target.build_configurations.each do |config|
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '17.0'
                config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
                config.build_settings['CODE_SIGNING_REQUIRED'] = 'NO'
                config.build_settings['ENABLE_BITCODE'] = 'NO'
                config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'
                config.build_settings['COMPILER_INDEX_STORE_ENABLE'] = 'NO'

                # 禁用用户脚本沙盒，防止 Cargokit / Rust 构建被 Xcode 拦截
                config.build_settings['ENABLE_USER_SCRIPT_SANDBOXING'] = 'NO'

                if target.name.include?('zstandard_ios')
                  config.build_settings['HEADER_SEARCH_PATHS'] ||= '$(inherited) '
                  config.build_settings['HEADER_SEARCH_PATHS'] << '"${PODS_TARGET_SRCROOT}/**" '
                  config.build_settings['DEFINES_MODULE'] = 'NO'
                end
              end

              # 修复 rust_lib_punklorde 重复输出
              if target.name.include?('rust_lib_punklorde')
                target.build_phases.each do |phase|
                  if phase.respond_to?(:output_paths)
                    phase.output_paths.clear
                  end
                end
              end
            end

            # 清洗 zstandard_ios umbrella
            umbrella_dir = "#{installer.sandbox.root}/Target Support Files/zstandard_ios"
            Dir.glob("#{umbrella_dir}/*umbrella.h").each do |umb|
              if File.exist?(umb)
                plugin_headers = Dir.glob("#{installer.sandbox.root}/zstandard_ios/Classes/*Plugin*.h")
                new_content = if plugin_headers.any?
                                plugin_headers.map { |h| "#import <zstandard_ios/#{File.basename(h)}>" }.join("\n")
                              else
                                "// Umbrella cleared\n"
                              end
                File.write(umb, new_content)
                puts "✅ post_install: Overwritten umbrella: #{umb}"
              end
            end
          end
          EOF

          echo "===== 统一部署目标并注入宏定义映射与构建优化参数 ====="
          python3 -c "
          import re, os, plistlib

          # 修改 Info.plist
          info_path = 'ios/Runner/Info.plist'
          if os.path.exists(info_path):
              try:
                  with open(info_path, 'rb') as f:
                      plist = plistlib.load(f)
                  plist['MinimumOSVersion'] = '17.0'
                  with open(info_path, 'wb') as f:
                      plistlib.dump(plist, f)
                  print('Updated Info.plist MinimumOSVersion to 17.0')
              except:
                  with open(info_path, 'r', encoding='utf-8') as f:
                      content = f.read()
                  content = re.sub(r'<key>MinimumOSVersion</key>\s*<string>[0-9.]+</string>', '<key>MinimumOSVersion</key><string>17.0</string>', content)
                  with open(info_path, 'w', encoding='utf-8') as f:
                      f.write(content)
                  print('Updated Info.plist MinimumOSVersion via regex.')

          # 修改 pbxproj，确保部署目标为 17.0，禁用索引并设置预处理器宏
          pbx_path = 'ios/Runner.xcodeproj/project.pbxproj'
          with open(pbx_path, 'r', encoding='utf-8', errors='ignore') as f:
              pbx = f.read()

          pbx = re.sub(r'IPHONEOS_DEPLOYMENT_TARGET\s*=\s*[0-9.]+;', 'IPHONEOS_DEPLOYMENT_TARGET = 17.0;', pbx)
          if 'DEVELOPMENT_TEAM =' in pbx:
              pbx = re.sub(r'DEVELOPMENT_TEAM\s*=\s*[^;]+;', 'DEVELOPMENT_TEAM = 1234567890;', pbx)
          else:
              pbx = re.sub(r'(buildSettings = \{)', r'\1\n\t\t\t\tDEVELOPMENT_TEAM = 1234567890;', pbx)

          if 'CODE_SIGNING_ALLOWED =' in pbx:
              pbx = re.sub(r'CODE_SIGNING_ALLOWED\s*=\s*[^;]+;', 'CODE_SIGNING_ALLOWED = NO;', pbx)
          else:
              pbx = re.sub(r'(buildSettings = \{)', r'\1\n\t\t\t\tCODE_SIGNING_ALLOWED = NO;', pbx)

          # 禁用 Xcode 脚本沙盒
          if 'ENABLE_USER_SCRIPT_SANDBOXING =' in pbx:
              pbx = re.sub(r'ENABLE_USER_SCRIPT_SANDBOXING\s*=\s*[^;]+;', 'ENABLE_USER_SCRIPT_SANDBOXING = NO;', pbx)
          else:
              pbx = re.sub(r'(buildSettings = \{)', r'\1\n\t\t\t\tENABLE_USER_SCRIPT_SANDBOXING = NO;', pbx)

          macro_setting = 'GCC_PREPROCESSOR_DEFINITIONS = ( \"$(inherited)\", \"FlutterForegroundTaskPlugin=SwiftFlutterForegroundTaskPlugin\", );'
          if 'GCC_PREPROCESSOR_DEFINITIONS =' in pbx:
              pbx = re.sub(r'GCC_PREPROCESSOR_DEFINITIONS\s*=\s*[^;]+;', macro_setting, pbx)
          else:
              pbx = re.sub(r'(buildSettings = \{)', r'\1\n\t\t\t\t' + macro_setting, pbx)

          with open(pbx_path, 'w', encoding='utf-8') as f:
              f.write(pbx)

          # 修改 xcconfig，注入 PATH 与编译参数
          override_flags = '\nPATH = $(HOME)/.cargo/bin:/usr/local/bin:/opt/homebrew/bin:$(PATH)\nIPHONEOS_DEPLOYMENT_TARGET = 17.0\nDEVELOPMENT_TEAM=1234567890\nCODE_SIGNING_ALLOWED=NO\nCODE_SIGNING_REQUIRED=NO\nCODE_SIGN_IDENTITY=\nCLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES=YES\nENABLE_BITCODE=NO\nCOMPILER_INDEX_STORE_ENABLE=NO\nENABLE_USER_SCRIPT_SANDBOXING=NO\nGCC_PREPROCESSOR_DEFINITIONS=$(inherited) FlutterForegroundTaskPlugin=SwiftFlutterForegroundTaskPlugin\n'
          for cfg in ['ios/Flutter/Generated.xcconfig', 'ios/Flutter/Release.xcconfig']:
              if os.path.exists(cfg):
                  with open(cfg, 'a', encoding='utf-8') as f:
                      f.write(override_flags)
          "

      - name: 8. CocoaPods 依赖缓存
        uses: actions/cache@v4
        with:
          path: |
            ios/Pods
            ~/.cocoapods/repos
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/Podfile') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: 9. 修复 zstandard_ios 并安装 CocoaPods 依赖
        run: |
          set -e

          echo "===== 1. 修复 zstandard_ios.podspec 语法位置 ====="
          python3 - <<'PY'
          import glob
          import re

          for ps in glob.glob('ios/.symlinks/plugins/zstandard_ios/**/*.podspec', recursive=True):
              print(f"Processing: {ps}")

              with open(ps, 'r', encoding='utf-8') as f:
                  content = f.read()

              # 清除既有的 public_header_files 設定
              content = re.sub(r's\.public_header_files\s*=\s*\[[^\]]*\]\s*', '', content, flags=re.DOTALL)
              content = re.sub(r's\.public_header_files\s*=\s*[^\n]*\n', '', content)

              # 確保 inserted 代碼在 end 之前（區塊內部）
              new_setting = "  s.public_header_files = 'Classes/**/*Plugin*.h'\n"
              if "Classes/**/*Plugin*.h" not in content:
                  if 'end' in content:
                      last_end_idx = content.rfind('end')
                      content = content[:last_end_idx] + new_setting + content[last_end_idx:]
                  else:
                      content += "\n" + new_setting

              with open(ps, 'w', encoding='utf-8') as f:
                  f.write(content)

          print("zstandard_ios podspec patch completed.")
          PY

          echo "===== 2. 执行 pod install ====="
          cd ios
          pod install --repo-update
          cd ..

      - name: 9.1 修正 PhaseScript 及 Pods 脚本执行权限
        run: |
          echo "===== 检查并修复所有 Shell 脚本的可执行权限 ====="
          chmod +x ios/Pods/Target\ Support\ Files/*/*.sh 2>/dev/null || true
          find ios/ -name "*.sh" -exec chmod +x {} \; 2>/dev/null || true

      - name: 10. 编译 iOS App（未签名）并保存详细日志
        run: |
          set -e

          echo "===== 开始构建（详细日志将保存到 build.log） ====="
          flutter build ios \
            --release \
            --no-codesign \
            --no-pub \
            -v 2>&1 | tee build.log

          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "构建失败，请查看 build.log"
            exit 1
          fi

      - name: 10.5 上传构建日志（无论成功与否）
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
        if: always()

      - name: 11. 打包为 IPA
        run: |
          set -e

          cd build/ios/iphoneos
          rm -rf Payload
          mkdir Payload
          mv Runner.app Payload/
          zip -r Runner.ipa Payload
          ls -lh Runner.ipa

      - name: 11.5 验证 IPA 是否包含百度地图资源包 (mapapi.bundle)
        run: |
          echo "===== 检查 IPA 包中是否存在 mapapi.bundle ====="
          if find build/ios/iphoneos/Payload -name "*mapapi*.bundle" | grep -q .; then
            echo "✅ SUCCESS: 成功在 IPA 中找到百度地图资源包 (mapapi.bundle)！"
            find build/ios/iphoneos/Payload -name "*mapapi*.bundle"
          else
            echo "⚠️ WARNING: 未在 IPA 中找到 mapapi.bundle，请确认 Pods 是否正常载入资源！"
          fi

      - name: 12. 上传 IPA
        uses: actions/upload-artifact@v4
        with:
          name: Punklorde-iOS-Unsigned
          path: build/ios/iphoneos/Runner.ipa